

class Response:
    def __init__(self, cookies,status_code,headers, body):
        cookies = self.cookies
        status_code = self.status_code
        headers = self.headers  
        body = self.body
        